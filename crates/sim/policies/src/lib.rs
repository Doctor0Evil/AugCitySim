pub struct PolicyConfig {
    pub community_veto_enabled: bool,
    pub symmetry_enforced: bool,
    pub hyper_transparency_on: bool,
    pub strict_no_coercion: bool,
    pub restorative_channels_on: bool,
    pub host_abort_required: bool,
}
